using System.Collections.Generic;
using UnityEngine;

namespace Game
{
    public class Snake
    {
        private LinkedList<Tile> snakeSegments;
        private Tile snakeHead;

        public Snake(Vector2Int startPosition, int startSize)
        {
            
        }
    }
}